#include <bits/stdc++.h>
using namespace std;

#define endl "\n"
#define int long long

void gen(vector<int>& nums, int num, int limit) {
    if (num > limit) return;
    if (num > 0) nums.push_back(num);
    gen(nums, num * 10 + 6, limit);
    gen(nums, num * 10 + 9, limit);
}

void solve(const vector<int>& nums) {
    int l, r;
    cin >> l >> r;
    int lm = lower_bound(nums.begin(), nums.end(), l) - nums.begin();
    int rm = upper_bound(nums.begin(), nums.end(), r) - nums.begin();
    cout << rm - lm << endl;
}

signed main() {
    cin.tie(0)->sync_with_stdio(0);
    cout << fixed << setprecision(9);

    vector<int> nums;
    int limit = 1e18;
    gen(nums, 0, limit);
    sort(nums.begin(), nums.end());
    int tt;
    cin >> tt;
    while (tt--) solve(nums);
    return 0;
}
