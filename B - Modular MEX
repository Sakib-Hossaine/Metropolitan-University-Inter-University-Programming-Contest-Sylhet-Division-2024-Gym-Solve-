#include<bits/stdc++.h>
using namespace std;
typedef long long ll;
const int N=1e6;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll t;
    cin >> t;
    while (t--) {
        ll x;
        cin >> x;
        cout << (x + 1) / 2 << "\n";
    }
    return 0;
}
